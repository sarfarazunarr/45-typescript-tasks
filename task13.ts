// Your Own Array: Think of your favorite mode of transportation, such as a motorcycle or a car, and make a list that stores several examples. Use your list to print a series of statements about these items, such as “I would like to own a Honda motorcycle.”
let myFavTransport: string[] = [
  "I wants to own a motorcar Rocco",
  "That car should be fast enough!",
  "The car's color must be black. ",
  "The car must be best in case of using fuel",
];
console.log(myFavTransport[0] + `\n`);
console.log(myFavTransport[1] + `\n`);
console.log(myFavTransport[2] + `\n`);
console.log(myFavTransport[3] + `\n`);
